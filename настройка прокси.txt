# === Настройка прокси ===
$proxyUrl = "http://user326857:6fhids@194.87.40.30:4513"
[System.Net.WebRequest]::DefaultWebProxy = New-Object System.Net.WebProxy($proxyUrl)
[System.Net.WebRequest]::DefaultWebProxy.Credentials = [System.Net.CredentialCache]::DefaultNetworkCredentials

# === Разрешаем выполнение скриптов для текущего пользователя ===
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser -Force

# === Установка factory droid CLI ===
irm https://app.factory.ai/cli/windows | iex

# === Проверка установки ===
factory droid --help

Удалить настройки аакаунта droid
Remove-Item "$env:USERPROFILE\.factory" -Recurse -Force
